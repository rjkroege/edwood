package frame

// Package frame supports frames of editable text in a single font on
// raster displays, such as in sam(1) and 9term(1). Frames may hold any
// character except NUL (0). Long lines are folded and tabs are at fixed
// intervals.
